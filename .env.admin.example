# Copia para .env.admin e preenche (o ficheiro .env.admin está no .gitignore).
# Usa a Service Role NOVA depois de regenerares a chave no Supabase (a antiga foi exposta).
# NUNCA uses EXPO_PUBLIC_ aqui — isto não vai para a app.

SUPABASE_URL=https://SEU_PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
